name: whiteboard_example
description: Demonstrates how to use the whiteboard plugin.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  whiteboard:
    path: ../
  tencent_im_sdk_plugin:
    path: /Users/vickyleu/Develop/GitRepo/tencent_im_sdk_plugin
#    git:
#      url: https://github.com/vickyleu/flutter_tim.git
#  tencent_trtc_cloud: 1.0.1
  cupertino_icons: 1.0.3

# The following section is specific to Flutter.
flutter:

  uses-material-design: true
